apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: serviceaccount
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/LetsGoPens87/k8s-argo-monitoring.git
    targetRevision: HEAD
    path: apps/serviceaccount
  destination:
    server: https://kubernetes.default.svc
    namespace: api
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
